import { Box } from '@mui/material';
import CartIconButton from 'src/components/shared/CartIconButton';

const Cart = () => {
  return (
    <Box>
      <CartIconButton color="inherit" />
    </Box>
  );
};

export default Cart;
